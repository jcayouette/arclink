# OpenTAKServer K3s Configuration
# Copy this file to config.env and adjust for your environment

# Primary node address (IP or DNS hostname where registry runs)
# Examples: 10.0.0.160 or node0.research.core
PRIMARY_NODE_ADDRESS=node0.research.core

# Docker registry address (usually PRIMARY_NODE_ADDRESS:5000)
# Examples: 10.0.0.160:5000 or node0.research.core:5000
REGISTRY_ADDRESS=node0.research.core:5000

# NodePort for web UI access (30000-32767)
WEB_NODEPORT=31080

# NodePort for TCP CoT connections (30000-32767)
TCP_COT_NODEPORT=31088

# NodePort for SSL CoT connections (30000-32767)
SSL_COT_NODEPORT=31089

# Kubernetes namespace
NAMESPACE=tak

# OpenTAKServer version
OTS_VERSION=1.6.3

# Database credentials (change for production!)
POSTGRES_PASSWORD=otspassword
POSTGRES_USER=ots
POSTGRES_DB=ots

# RabbitMQ credentials (change for production!)
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest

# Flask Security Secrets (CRITICAL - DO NOT SHARE!)
# These are auto-generated by configure.sh
# To generate manually:
#   SECRET_KEY: openssl rand -hex 32
#   SECURITY_PASSWORD_SALT: openssl rand -base64 32
SECRET_KEY=your-secret-key-here-run-configure-sh-to-generate
SECURITY_PASSWORD_SALT=your-password-salt-here-run-configure-sh-to-generate
